<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Medio</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/aboutus">About Us</a>
        </li>
        
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to "Profile", user_path(current_user), class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Followers", "/users/#{current_user.id}/followers", class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Following", "/users/#{current_user.id}/following", class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Meet Up!", "/users/#{current_user.id}/meet_view", class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Log Out", logout_path, class: "nav-link active", method: 'delete' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log In", login_path, class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", signup_path, class: "nav-link active" %>
          </li>
        <% end %>
      </ul>
      <%
=begin%>
<form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form> 
<%
=end%>
    </div>
  </div>
</nav>
